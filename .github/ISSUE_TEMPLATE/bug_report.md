---
name: Bug Report
about: Report a bug in the junction CLI, SDK, or a scaffolded agent/skill
title: "[Bug] "
labels: bug
assignees: gagan-shangari-egenai
---

## Description

A clear and concise description of the bug.

## Steps to Reproduce

1. Command run (e.g. `junction --discover --defaults --agent claude-code --target ./repo`):
2. Answers file or flags used:
3. Expected behavior:
4. Actual behavior:

## Environment

- Python version:
- `junction --version`:
- `--agent`: `copilot` / `claude-code` / `cursor` / `continue` / `agnostic`
- `--cloud` / `--iac`:

## Logs / Output

```
Paste relevant CLI output, tracebacks, or agent-graph validation errors here
```

## Additional Context

Add any other context, or `agent-graph.yml` / `platform.yml` snippets (redact secrets/account IDs).
